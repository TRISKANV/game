<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
        Base theme used before Compose takes over.
        Uses AppCompat (always available) instead of android:Theme.Material
        which is not available in all CI SDK configurations.
    -->
    <style name="Theme.KotlinSurvivors" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@color/splash_bg</item>
        <item name="android:statusBarColor">@color/splash_bg</item>
        <item name="android:navigationBarColor">@color/splash_bg</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <!-- Splash screen theme — shown while app loads -->
    <style name="Theme.KotlinSurvivors.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_bg</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_icon</item>
        <item name="windowSplashScreenAnimationDuration">300</item>
        <item name="postSplashScreenTheme">@style/Theme.KotlinSurvivors</item>
    </style>

</resources>
